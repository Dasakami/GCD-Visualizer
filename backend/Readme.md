# 🧮 GCD Visualizer Backend

Образовательный веб-сервис для визуализации алгоритма Евклида (поиск НОД).

## 🚀 Возможности

- ✅ **Пошаговая визуализация** алгоритма Евклида
- ✅ **JWT авторизация** (регистрация и вход)
- ✅ **История вычислений** для каждого пользователя
- ✅ **Теоретические материалы** об алгоритме
- ✅ **PostgreSQL база данных**
- ✅ **Docker контейнеризация**
- ✅ **Swagger документация** из коробки

## 📁 Структура проекта

```
gcd-visualizer/
├── app/
│   ├── main.py                      # Главный файл приложения
│   ├── core/
│   │   ├── config.py               # Настройки приложения
│   │   └── security.py             # JWT логика
│   ├── db/
│   │   ├── base.py                 # Подключение к БД
│   │   └── models.py               # SQLAlchemy модели
│   ├── routers/
│   │   ├── auth_router.py          # Регистрация/вход
│   │   ├── gcd_router.py           # Алгоритм Евклида
│   │   └── theory_router.py        # Теория
│   ├── services/
│   │   └── euclid_service.py       # Бизнес-логика
│   └── schemas/
│       └── gcd_schema.py           # Pydantic схемы
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── .env
└── README.md
```

## 🛠 Технологии

- **Python 3.11**
- **FastAPI** - современный веб-фреймворк
- **PostgreSQL 15** - база данных
- **SQLAlchemy** - ORM
- **Pydantic** - валидация данных
- **JWT** - авторизация
- **Docker** - контейнеризация

## 📦 Установка и запуск

### Вариант 1: С Docker (рекомендуется)

1. Склонируй проект и перейди в директорию:
```bash
cd gcd-visualizer
```

2. Создай файл `.env` (можно скопировать из примера выше)

3. Запусти проект:
```bash
docker-compose up --build
```

4. API будет доступно по адресу: **http://localhost:8000**
5. Swagger документация: **http://localhost:8000/docs**

### Вариант 2: Без Docker

1. Установи PostgreSQL и создай базу данных `gcd_db`

2. Установи зависимости:
```bash
pip install -r requirements.txt
```

3. Настрой `.env` файл с правильным DATABASE_URL

4. Запусти приложение:
```bash
uvicorn app.main:app --reload
```

## 📚 API Endpoints

### 🔐 Авторизация

#### `POST /auth/register`
Регистрация нового пользователя
```json
{
  "email": "user@example.com",
  "password": "password123"
}
```

#### `POST /auth/login`
Вход и получение JWT токена
```json
{
  "email": "user@example.com",
  "password": "password123"
}
```
**Ответ:**
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "token_type": "bearer"
}
```

### 🧮 Алгоритм Евклида

#### `POST /gcd/calculate`
Вычисление НОД с пошаговой визуализацией
```json
{
  "a": 48,
  "b": 18
}
```
**Ответ:**
```json
{
  "steps": [
    {"a": 48, "b": 18, "quotient": 2, "remainder": 12},
    {"a": 18, "b": 12, "quotient": 1, "remainder": 6},
    {"a": 12, "b": 6, "quotient": 2, "remainder": 0}
  ],
  "result": 6
}
```
**Требуется авторизация!** Добавь заголовок:
```
Authorization: Bearer YOUR_JWT_TOKEN
```

#### `GET /gcd/history`
Получить историю вычислений
```
GET /gcd/history?limit=10
```

#### `GET /gcd/history/{result_id}`
Получить конкретный результат

#### `DELETE /gcd/history/{result_id}`
Удалить результат из истории

### 📖 Теория

#### `GET /theory/euclid`
Получить описание алгоритма Евклида

#### `GET /theory/complexity`ч
Информация о сложности алгоритма

#### `GET /theory/applications`
Практические применения алгоритма

## 🔧 Тестирование через Swagger

1. Открой **http://localhost:8000/docs**
2. Зарегистрируй пользователя через `/auth/register`
3. Войди через `/auth/login` и скопируй токен
4. Нажми кнопку **Authorize** (🔒) вверху страницы
5. Вставь токен в формате: `Bearer YOUR_TOKEN`
6. Теперь можешь тестировать все защищённые эндпоинты!

## 🗃 База данных

### Модель User
- `id` - уникальный идентификатор
- `email` - email пользователя (уникальный)
- `hashed_password` - хешированный пароль
- `created_at` - дата регистрации

### Модель GCDResult
- `id` - уникальный идентификатор
- `user_id` - связь с пользователем
- `a`, `b` - входные числа
- `result` - результат НОД
- `steps` - JSON с шагами алгоритма
- `created_at` - дата вычисления

## 🎯 Примеры использования

### Пример на Python (requests)
```python
import requests

# 1. Регистрация
response = requests.post("http://localhost:8000/auth/register", json={
    "email": "student@example.com",
    "password": "securepass"
})

# 2. Вход
response = requests.post("http://localhost:8000/auth/login", json={
    "email": "student@example.com",
    "password": "securepass"
})
token = response.json()["access_token"]

# 3. Вычисление НОД
headers = {"Authorization": f"Bearer {token}"}
response = requests.post("http://localhost:8000/gcd/calculate", 
    json={"a": 48, "b": 18},
    headers=headers
)
print(response.json())

# 4. История
response = requests.get("http://localhost:8000/gcd/history", headers=headers)
print(response.json())
```

### Пример на JavaScript (fetch)
```javascript
// 1. Регистрация
const register = await fetch('http://localhost:8000/auth/register', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({
    email: 'student@example.com',
    password: 'securepass'
  })
});

// 2. Вход
const login = await fetch('http://localhost:8000/auth/login', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({
    email: 'student@example.com',
    password: 'securepass'
  })
});
const { access_token } = await login.json();

// 3. Вычисление НОД
const calculate = await fetch('http://localhost:8000/gcd/calculate', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${access_token}`
  },
  body: JSON.stringify({ a: 48, b: 18 })
});
const result = await calculate.json();
console.log(result);
```

## 🔒 Безопасность

- Пароли хешируются с помощью **bcrypt**
- JWT токены с настраиваемым временем жизни
- CORS настроен для работы с фронтендом
- В продакшене обязательно смени `SECRET_KEY` в `.env`!

## 📝 TODO / Возможные улучшения

- [ ] Добавить rate limiting
- [ ] Добавить логирование действий
- [ ] Добавить email верификацию
- [ ] Добавить расширенный алгоритм Евклида
- [ ] Добавить экспорт истории в CSV/PDF
- [ ] Добавить тесты (pytest)
- [ ] Добавить Redis для кэширования

## 🎓 Для защиты проекта

Можешь рассказать комиссии:

1. **Архитектура**: Многослойная (routers → services → models)
2. **База данных**: Реляционная PostgreSQL с миграциями через SQLAlchemy
3. **Безопасность**: JWT авторизация, хеширование паролей
4. **API**: RESTful принципы, автодокументация Swagger
5. **Развёртывание**: Docker контейнеры для простого деплоя
6. **Масштабируемость**: Легко добавлять новые алгоритмы и функции

## 📧 Контакты

Если нужна помощь - пиши!

---

**Удачи на защите! 🎉**